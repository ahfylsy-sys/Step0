# 2026-04-12 工作日志

## 模型配置清单生成

- 在 `Step0/MODEL_CONFIG.md` 生成了完整的模型配置与假设清单
- 覆盖18个分类、100+条配置项，每条标注当前值、代码位置、文献依据
- 该文件设计为"唯一配置真相源"，刘兄修改后小佑读取并同步更新代码
- 标注了5个已知不一致项（A1-A5），包括NSGA2参数与论文Table 2的不一致
- 另生成叙述式配置清单 `MODEL_CONFIG_NARRATIVE.md`（70条逐条文字叙述），便于刘兄宏观把控修改

## 刘兄修改MODEL_CONFIG_NARRATIVE.md后的代码同步

- 刘兄修改了5个区域：起始总述、第1节(源项/EPZ)、第5节(道路等级)、第6节(动态关闭)、第11节(NSGA2参数确认)
- 代码已在之前会话中同步完成：config.py新增ROAD_HIERARCHY_CONFIG和PICKUP_CLOSURE_CONFIG，data_loader.py增加道路等级识别和污染时间预计算，optimizer.py增加动态关闭重定向逻辑，main.py增加--no-dynamic-closure CLI
- 审核了动态关闭方案：到达前重定向+到达后留等，确认合理
- 文档更新：item 20替换为确认后的正式描述，item 44移除⚠️确认标记，待确认项清空
- 创建CHANGELOG.md修改日志文件
- 同步更新MODEL_CONFIG.md表格版（新增5.8-5.11道路等级、6.5-6.9动态关闭、1.3-1.6源项/EPZ等）
- 优化日志：optimize_group开头记录关键配置摘要（sink/shelter/closure状态、NSGA2参数、源项/EPZ/道路等级）
- 版本升级至v5.4

## 代码-文档一致性审核

- 逐条对比 MODEL_CONFIG_NARRATIVE.md（70条）与10个Python文件的全部配置
- 发现并修复4处不一致：
  1. item 8：原文"Phase 2巴士运输逐分钟插值"与代码不符→改为"步行阶段逐分钟插值+巴士仅排队风险"
  2. item 17重复编号（第5节和第6节都有17）→第6节改为18起，后续顺延
  3. item 33（原31）：容量范围"10~5000人"与"过滤后567个"混在一起→明确为"过滤前"
  4. item 34（原32）：KDTree候选数描述不精确→区分multi模式(max(2K,100)=100)和static模式(50)
- 修复后总条目数从70→70（编号1-17,18-70连续无间断）

## Claude Code全量审核

- 使用Claude Code CLI对70条配置进行逐条代码对比验证
- 结果：67条✅ / 2条不一致 / 1条数据相关无法验证
- 发现并修复2处不一致：
  1. item 20：文档称"pkl缓存避免重复计算"，实际代码仅检查pkl是否存在作为前置条件，可行域由build_feasible()实时计算→修正文档描述
  2. item 57：文档默认选择策略为min_risk，但data_loader.py:102硬编码selection_method="min_time"→代码改为"min_risk"
- 刘兄要求后续默认使用claude-code skill进行分析

## v5.5 多阶段滚动时域优化实现

- 刘兄提出动态多阶段撤离决策需求：t=0依据15min风险矩阵禁用非零风险上车点，t=15/25/35重新优化
- 使用Claude Code分析3种方案：A(滚动时域分解), B(全局多阶段编码), C(单次运行+重评估)
- 推荐方案A（综合25/30分），方案B(16/30)解空间爆炸，方案C(22/30)学术价值弱
- 完成方案A代码实现：
  - config.py: 新增MULTISTAGE_CONFIG
  - data_loader.py: 新增rebuild_feasible_for_stage()、compute_intermediate_positions()
  - optimizer.py: 新增hot_start_quantum_population()、make_evaluate_stage()
  - main.py: 新增optimize_group_multistage()、--multistage CLI
  - visualization.py: plot_assignment_map()新增suffix参数
- 新建MULTISTAGE_DESIGN.md技术分析报告
- 版本升级至v5.5

## v5.6 四阶段安全约束实现

- 刘兄提出新想法：预计算15/25/35/45min安全上车点列表，优化中验证居民到达时刻安全性
- 实现四阶段安全约束 (4-Stage Safety Constraint)：
  - data_loader.py: 新增 compute_4stage_safe_stops() 生成 (n_bus, 4) 安全掩码
  - optimizer.py: make_evaluate() 新增 stage_safe_masks 参数及到达阶段安全检查
  - main.py: 新增 use_4stage_safe 参数、--4stage-safe CLI、Step 3.1掩码计算、Step 3.2可行域过滤
- 启用4阶段安全时自动禁用动态关闭（冗余+过度约束）
- 首次运行3/12分组失败→修复Step 3.2 bug→12/12全部成功
- 运行结果：1320s，12/12分组，17,756人口，12/26上车点使用，步行1.0~37.9min
- 生成 V56_FOUR_STAGE_SAFETY_REPORT.md 分析报告
- 更新 MODEL_CONFIG_NARRATIVE.md 新增 items 77-82
- 更新 CHANGELOG.md 新增 v5.6 变更记录
- 确认 main.py 中 export_results_excel 和 elapsed 重复计算 bug 已不存在（之前已修复）
- 版本升级至v5.6
