# 2026-04-15 工作日志

## v7.1 关键修复完成

### 修改文件清单
1. **config.py** — 新增 PICKUP_RISK_FILTER (enabled=True, risk_threshold=0.0) 和 ROAD_DISTANCE_FACTOR=1.3
2. **data_loader.py** — 新增 filter_stops_by_risk() 函数, 排除t≤15min被风险覆盖的上车点
3. **pickup_sink.py** — 修正 _precompute_road_distances(): stop↔shelter改用Euclidean×1.3替代错误的Dijkstra (避难所≥30km超出路网裁剪范围)
4. **main.py** — Step 1.5集成上车点风险过滤; 导入filter_stops_by_risk

### 四大问题修复状态
1. ✅ 居民选择高风险上车点 → filter_stops_by_risk() 排除
2. ✅ 避难所距离偏低 → Euclidean×1.3修正
3. ✅ 巴士返程逻辑 → v7.0已正确实现(之前看起来不返程是因距离偏低)
4. ✅ 可视化(避难所过滤+CartoDB底图) → 已实现

### 测试结果
- m_20-29组: 20/26安全站点, 575/678避难所, 200/200可行解
- 160代优化完成, 疏散时间292.4min, 7站点
- 动画+快照+Excel输出正常

### 运行注意
- Windows需: $env:PYTHONIOENCODING="utf-8"
